export type Unique = {
  [index: string]: string | number;
};

export type State = {
  [index: string]: string | number;
};

export type CardList = {
  firstName: string;
  secondName: string;
  email: string;
  country: string;
  gender: string;
  date: string;
  photo: string;
};
